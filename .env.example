# Internal Integration Token from the "mind-your-noteness" connection
# (notion.so/my-integrations → mind-your-noteness → Internal Integration Token).
# Starts with secret_ or ntn_. Never commit the real .env.
NOTION_TOKEN=

# Optional: port for the local server (default 8765)
# PORT=8765
